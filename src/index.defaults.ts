//  @namespace Phaser

import Device from './device/index.defaults';
import DOM from './dom/index.defaults';
import Game from './Game';
import Loader from './loader/index.defaults';

const Phaser = {
    Game,
    Device,
    DOM,
    Loader
};

export default Phaser;
