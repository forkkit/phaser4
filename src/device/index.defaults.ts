//  @namespace Phaser.Device

import * as Audio from './audio/index.defaults';
import * as Browser from './browser/index.defaults';
import * as OS from './os/index.defaults';
import * as Video from './video/index.defaults';

const Device = {
    Audio,
    Browser,
    OS,
    Video
};

export default Device;
